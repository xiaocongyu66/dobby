
# Clean Build Notes

This package keeps the legacy stealth and substrate-compatible sources in the tree for reference,
but the main runtime path is now the unified Dobby runtime.

The project root CMake no longer exposes the stealth toggle.

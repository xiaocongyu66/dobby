#pragma once

// Compatibility wrapper kept for older ARM/x86 source files which still
// include core/arch/Cpu.h. The current register base definition lives in
// CpuRegister.h.
#include "core/arch/CpuRegister.h"
